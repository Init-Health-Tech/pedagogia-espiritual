# Django
SECRET_KEY=genera-un-secret-key-seguro-aqui
DEBUG=False
ALLOWED_HOSTS=api.pedagogia-espiritual.init.com.mx,localhost,127.0.0.1

# CORS — dominio del frontend en Vercel
CORS_ALLOWED_ORIGINS=https://pedagogia-espiritual.init.com.mx
CSRF_TRUSTED_ORIGINS=https://pedagogia-espiritual.init.com.mx,https://api.pedagogia-espiritual.init.com.mx

# Base de datos (PostgreSQL en la VM)
DB_ENGINE=postgresql
DB_NAME=pedagogia_espiritual
DB_USER=pedagogia
DB_PASSWORD=cambia-esta-contrasena
DB_HOST=localhost
DB_PORT=5432

# SSL (nginx + certbot)
SECURE_SSL_REDIRECT=True
