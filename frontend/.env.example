# Puerto del backend local (solo desarrollo; el proxy de Vite lo usa)
VITE_BACKEND_PORT=8005

# En producción (Vercel) apunta al API en DigitalOcean:
# VITE_API_URL=https://api.pedagogia-espiritual.init.com.mx/api
